rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permit read and write access to all collections for e-Mudra Aadhaar Center app
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
